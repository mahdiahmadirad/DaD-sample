# TASK-0002: Align task documents with the current DaD convention

## Status

Complete

## Outcome

Align the repository with the current official DaD document layout by keeping numbered task documents under `docs/tasks/` and updating every repository reference to their canonical paths.

## In scope

- Move the existing `TASK-0001` history from the repository root to `docs/tasks/TASK-0001.md` without changing its identifier or completion record.
- Add this migration task as `docs/tasks/TASK-0002.md`.
- Update repository guidance, the repository map, and ADR/SPEC traceability links that refer to task documents or their location.
- Verify that no governed task document or stale task-path reference remains at the repository root.
- Run the repository build and test checks.

## Out of scope

- Changes to API behavior, application code, tests, or build tooling.
- Adding DaD CLI support files or introducing DaD CLI usage to the sample.
- Rewriting the content or history of completed tasks beyond the relative-link changes required by their new location.

## Constraints

- Preserve existing task identifiers, lifecycle status, and completion evidence.
- Keep `docs/adr/`, `docs/specs/`, and `docs/tasks/` as the canonical numbered-document locations.
- Use `docs/tasks/TASK-NNNN.md` in repository guidance and examples.

## Acceptance conditions

- No `TASK-NNNN.md` document remains at the repository root.
- `TASK-0001` exists at `docs/tasks/TASK-0001.md` with its original identifier, status, and completion evidence preserved.
- Repository-local links to task documents resolve from their new locations.
- AGENTS and README describe `docs/tasks/` as the canonical task location.
- Searches find no stale reference to the former root-level task location.
- `./scripts/build.sh` succeeds.
- `./scripts/test.sh` succeeds.

## Completion evidence

- Moved the completed `TASK-0001` document to `docs/tasks/TASK-0001.md` while preserving its identifier, lifecycle status, and completion evidence.
- Added this migration record at `docs/tasks/TASK-0002.md` and reconciled the task-location guidance and repository map in `AGENTS.md` and `README.md`.
- Updated the task links in `docs/adr/ADR-0001.md` and `docs/specs/SPEC-0001.md`; an automated local-link scan confirmed that every repository-local Markdown link resolves.
- Confirmed that no numbered task document remains at the repository root and that no stale root-level task reference remains in tracked content.
- `./scripts/build.sh` succeeded with .NET SDK 10.0.400: 0 warnings and 0 errors.
- `./scripts/test.sh` succeeded with .NET SDK 10.0.400: 4 passed, 0 failed, 0 skipped.
